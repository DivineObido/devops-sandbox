events {
    worker_connections 1024;
}

http {
    include /etc/nginx/conf.d/*.conf;

    server {
        listen 80;
        server_name _;

        location / {
            return 200 'DevOps Sandbox Platform is running\n';
            add_header Content-Type text/plain;
        }
    }
}